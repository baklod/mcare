@extends('trainer.layouts.app', ['title' => 'Certificates | MCARE Trainer'])
@section('content')
<div class="mx-auto max-w-7xl space-y-7"><header class="border-b border-stone-200 pb-6"><p class="text-sm font-bold uppercase tracking-[0.16em] text-violet-700">Certificates</p><h1 class="mt-2 text-3xl font-bold text-stone-950">Completion eligibility</h1><p class="mt-2 text-stone-600">Trainer review view; final certificate issuance remains controlled by the admin.</p></header><div class="border border-stone-200 bg-white">@forelse($trainees as $trainee)<div class="flex flex-col gap-3 border-b border-stone-100 p-5 sm:flex-row sm:items-center sm:justify-between"><div><p class="font-bold text-stone-950">{{ $trainee->first_name }} {{ $trainee->last_name }}</p><p class="text-sm text-stone-500">{{ $trainee->batch?->trainingStateLabel() ?? 'No training schedule' }}</p></div><span class="w-fit px-3 py-1 text-xs font-bold {{ $trainee->batch?->trainingState() === 'completed' ? 'bg-emerald-50 text-emerald-800' : 'bg-stone-100 text-stone-600' }}">{{ $trainee->batch?->trainingState() === 'completed' ? 'For admin certificate review' : 'Training in progress' }}</span></div>@empty<p class="p-6 text-stone-600">No approved trainees found.</p>@endforelse</div></div>
@endsection
